
task :default => [:run_app]

desc 'run sample_app.rb'
task :run_app   do
  puts 'running app...'
  app_path = "sample_app.rb"
  pdf_path = "output.pdf"
  system "cd . && ruby #{app_path}"
  system "cd . && ruby #{app_path}"
  
end